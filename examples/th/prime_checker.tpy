กำหนด จำนวนเฉพาะ(น: จำนวนเต็ม):
    ถ้า น <= 1:
        คืนค่า เท็จ

    วนซ้ำ จ ใน ช่วง(2, น):
        ถ้า น % จ == 0:
            คืนค่า เท็จ
    คืนค่า จริง

จำนวน: จำนวนเต็ม = จำนวนเต็ม(ป้อนค่า('กรอกหมายเลข: '))
แสดงผล(จำนวนเฉพาะ(จำนวน))